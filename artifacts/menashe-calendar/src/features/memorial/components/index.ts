// ── Memorial Components barrel ────────────────────────────────────────────────
// Components are defined in SPR-015 (UI implementation sprint).
// This barrel is intentionally empty until that sprint lands.
//
// Planned exports (to be implemented):
//   MemorialCard         — compact memorial summary card for search results
//   MemorialProfile      — full profile header with name, dates, biography
//   CandleWall           — grid of lit candles with light action
//   TributeList          — paginated tribute stream with moderation controls
//   PhotoGallery         — responsive photo grid with upload affordance
//   FamilyRoster         — family members list with role management
//   CreateMemorialWizard — multi-step creation form
//   MemorialSearchBar    — debounced search input
//   PermissionGate       — renders children only if the user has a given permission
